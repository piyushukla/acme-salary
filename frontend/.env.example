VITE_API_URL="http://localhost:4000"
# Demo-only: single HR token. In prod this would be a session cookie / server proxy,
# not a token shipped to the browser (flagged as out-of-scope in REQUIREMENTS).
VITE_API_TOKEN="dev-hr-token-change-me"
